# 내부저항 읽기 루틴

이 문서는 현재 펌웨어(`src/main.cpp`) 기준으로 내부저항 측정 시나리오를 정리한 내용이다.

## 1) 내부저항은 항상 읽지 않고, 주기 조건에서 읽는다

핵심 의도는 전압 측정과 달리 내부저항을 매 순간 읽지 않고, 조건이 맞을 때만 읽는 것이다.

- **일반 모드(`MEASURE_TEST_COMBINED_VZ = 0`)**
  - 내부저항 라운드는 `systemDefaultValue.ImpedanceMeasurePeriod`(초) 주기로 시작한다.
  - 값이 `0`이면 기본값 `IMP_MEASURE_PERIOD_DEFAULT_SEC`(현재 3600초)를 사용한다.
  - 구현 함수:
    - `impedanceMeasurePeriodMs()`
    - 루프 조건: `if ((now - lastImpedancePeriodMs) >= impPeriodMs)`

- **현재 빌드 테스트 모드(`MEASURE_TEST_COMBINED_VZ = 1`)**
  - 전압 주기(`CELL_VOLTAGE_INTERVAL_MS`, 현재 3000ms)에 맞춰 셀 순환하며 V+Z를 같이 읽는다.
  - 즉, 분리 주기 대신 3초 루프에 묶여 내부저항을 읽는다.

## 2) 연속 샘플을 읽고, 안정화 구간부터 유효값을 계산한다

한 셀의 내부저항 측정은 `readCellImpedanceWithWarmup()`에서 수행된다.

- 측정 전 조건:
  - 셀 전압이 `CELL_VOLTAGE_IMP_VALID_MIN_V`(현재 0.6V) 미만이면 내부저항 측정을 건너뛴다.

- 측정 절차:
  1. 최대 `readMax`회 연속 샘플 읽기  
     (`readMax = impedanceReadMax()`, 웹 설정: `impedanceReadMax`)
  2. 각 샘플은 `impSampleUsable()` 조건을 통과해야 유효 샘플로 채택
     - `Real > 0`
     - `|Z| >= impedanceMinValidMohm()`  
       (웹 설정: `impedanceMinValidMohm`)
  3. 유효 샘플이 `stableWindow`개 쌓이면 안정성 검사
     - 검사 함수: `impWindowIsStable()`
     - 조건:
       - 창의 첫/끝 샘플 상대 변화율 <= 허용치
       - 창 내부 `(max-min)/denom <= 허용치`
       - 허용치: `impedanceStableToleranceRatio()`  
         (웹 설정: `impedanceStableTolPercent`)
  4. 안정 창이 잡히면 추가 `postStableSamples`개를 더 읽어 평균 계산
     - 웹 설정: `impedancePostStableSamples`
  5. 그 평균값을 최종 내부저항으로 채택하고 런타임 값(`cellvalue`)에 반영

## 3) 정해진 횟수까지 안정화 실패 시 EEPROM 기준값을 사용한다

`readMax`까지 안정 창을 못 찾으면 다음 순서로 폴백한다.

1. `applyCellImpedanceFromEeprom()`로 해당 셀 EEPROM 기준 내부저항 적용 시도
2. 적용 후 값이 유효하면 그 값을 사용
3. EEPROM 값도 없으면 직전 런타임 값(`prevZ`) 유지

즉, 안정화 실패 시 측정 노이즈값을 억지로 쓰지 않고 **EEPROM 기준값 우선**으로 동작한다.

## 4) 최종 변화가 일정 비율 이상일 때만 EEPROM에 기록한다

유효한 최종 내부저항이 계산되더라도, EEPROM은 항상 쓰지 않는다.

- 함수: `tryPersistCellImpedanceToEeprom()`
- 기준값:
  - `systemDefaultValue.ImpedanceFactor`(%), 웹 설정: `impedanceEepromChangePercent`
- 판단:
  - 기존 EEPROM 값이 있을 때:
    - `|new-old| >= old * (ImpedanceFactor/100)`일 때만 저장
  - 기존 EEPROM 값이 없을 때(`old <= 0`):
    - 바로 저장 허용

이 정책으로 잦은 소폭 변동에 의한 EEPROM 마모를 줄인다.

## 웹 설정값과 내부 매핑

웹 `BMS 제어`의 항목은 아래 내부 필드로 매핑된다.

- `impedanceMeasurePeriodSec` -> `ImpedanceMeasurePeriod`
- `impedanceEepromChangePercent` -> `ImpedanceFactor`
- `impedanceReadMax` -> `ACVoltPP`
- `impedanceStableWindow` -> `DCVolt`
- `impedanceStableTolPercent` -> `VoltageFactor`
- `impedancePostStableSamples` -> `TemperatureFactor`
- `impedanceMinValidMohm` -> `RcalLoopCount`(x10 deci-mOhm)

## 요약

질문하신 시나리오는 맞다.

1. 주기 조건에서만 읽고  
2. 연속 샘플 중 안정화 구간부터 평균을 계산하며  
3. 안정화 실패 시 EEPROM 기준값으로 폴백하고  
4. 변화량이 임계치 이상일 때만 EEPROM을 갱신한다.

추가로, 현재는 `MEASURE_TEST_COMBINED_VZ = 1`이라 내부저항이 전압 3초 루프에 함께 묶여 동작한다는 점만 운영 시 주의하면 된다.
